{
  "name": "chrome-bookmark-list",
  "version": "1.0.0",
  "description": "Chrome extension for displaying bookmarks in a folder-based list",
  "scripts": {
    "build": "tsc",
    "build:extension": "./build.sh",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5"
  },
  "private": true
}
